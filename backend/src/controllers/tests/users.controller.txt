import request from "supertest";
import { app } from "../../app";

describe("Adding/Removing follower or following", async () => {
  test("addFollower (POST /api/users/:userId/followers)", async () => {
    const response = await request(app)
      .post("/api/users/:userId/followers")
      .send({});
  });
});
